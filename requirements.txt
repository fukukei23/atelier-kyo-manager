# requirements.txt
# 最終更新: 2026-05-10

# --- Flask Core ---
Flask>=3.0,<4.0
Flask-SQLAlchemy>=3.1,<4.0
Flask-WTF>=1.2,<2.0
flask_migrate>=4.0,<5.0
Flask-Login>=0.6,<1.0
python-dotenv>=1.0,<2.0

# --- Browser & Scraping ---
playwright>=1.40,<2.0
beautifulsoup4>=4.12,<5.0
requests>=2.31,<3.0

# --- Image Processing ---
Pillow>=10.0,<11.0
opencv-python-headless>=4.9,<5.0
numpy>=1.26,<2.0
rembg>=2.0,<3.0
onnxruntime>=1.17,<2.0  # rembg の ML runtime（CI依存漏れ解消）
icrawler>=0.2,<1.0
pixelmatch>=0.3,<1.0

# --- Observability ---
opentelemetry-api>=1.20,<2.0
diskcache>=5.6,<6.0

# --- Async Tasks (Celery) ---
celery>=5.3,<6.0

# --- Data Models & HTTP Client ---
pydantic>=2.0,<3.0
httpx>=0.27,<1.0

# --- Data Handling ---
pandas>=2.1,<3.0

# --- Data Handling ---
# (pandas は実使用ゼロのため削除・2026-09-06 ISSUE依存不整合)

# --- Testing ---
pytest>=7.4,<9.0
pytest-asyncio>=0.23,<1.0
pytest-cov>=4.1,<6.0
anyio>=4.0,<5.0  # @pytest.mark.anyio 用（CI依存漏れ解消）
