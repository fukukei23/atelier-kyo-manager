# 自動修正ループの使い方

## 概要

WSL環境でのログファイルを自動的に読み取り、問題を分析し、修正案を提案するシステムです。

## 使い方

### 1. ログ解析スクリプトの実行

```bash
cd /home/yn441611/atelier-kyo-manager
source venv/bin/activate
python auto_analyze_and_fix.py
```

このスクリプトは：
- 最新の `browser_test_*.log` を自動的に検索
- ログを解析して問題を特定
- 修正案を提案
- レポートを `log_analysis_YYYYMMDD_HHMMSS.md` に保存

### 2. 自動修正ループ（将来実装予定）

```bash
python auto_fix_and_retry.py
```

このスクリプトは：
- テストを実行
- ログを自動的に解析
- 問題を検出
- 修正案を生成
- 自動修正可能な場合は適用
- 再実行をループ

## 現在の実装状況

### 実装済み

1. **ログ解析スクリプト** (`auto_analyze_and_fix.py`)
   - ✓ ログファイルの自動検索
   - ✓ エラーパターンの検出
   - ✓ 修正案の生成
   - ✓ レポートの生成

2. **セレクタエラーの修正** (`navigation_driver.py`)
   - ✓ `click_first_card_or_link` のタイムアウト処理を改善
   - ✓ セレクタが見つからない場合のエラーハンドリングを改善

### 将来の実装

1. **完全自動修正ループ**
   - ログ解析 → 問題検出 → 修正案生成 → 自動適用 → 再実行
   - 手動確認が必要な場合は停止

2. **修正案の自動適用**
   - セレクタの更新
   - 設定ファイルの更新
   - コードの自動修正（慎重に）

## 検出される問題

### 1. PDP リンクが0件
- **検出**: `Collected 0 PDP links`
- **修正案**: `pdp_link_selectors` の確認

### 2. Materialization 失敗
- **検出**: `PLP materialization failed`
- **修正案**: `ensure_plp_materialized` の条件調整

### 3. セレクタが見つからない
- **検出**: `waiting for locator(...)`
- **修正案**: セレクタの更新（既に修正済み）

### 4. タイムアウト
- **検出**: `Timeout after ...s`
- **修正案**: タイムアウト時間の調整

## 注意事項

- 完全自動化は難しいため、重要な修正は手動確認が必要
- ログファイルは自動的に検索されるため、コピペは不要
- レポートは自動的に生成されるため、結果を確認しやすい

## 次のステップ

1. ログ解析スクリプトを実行して、現在の問題を確認
2. 修正案に基づいて、手動で修正
3. 再実行して、問題が解決したか確認

