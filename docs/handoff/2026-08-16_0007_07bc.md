# 引き継ぎ

## メタ情報
- session_id: 07bcb873-a2dd-52ff-9113-8d9d618d731c
- wt_session: 07bcb873-a2dd-52ff-9113-8d9d618d731c （WT_SESSION 未設定のため SESSION_ID で代替）
- セッション名: unknown （active-sessions.md がリモート環境に存在せず取得不可）
- 終了: 2026-08-16 00:07

> ⚠️ **本 handoff はリモート実行環境（Claude Code on the web）で生成された。**
> `~/projects/obsidian-ssot/` も `~/projects/claude-config/` も存在せず、
> SSOT 由来の情報（日記・DECISIONS・バックログ・active-sessions）は**一切取得できていない**。
> 内容は `fukukei23/atelier-kyo-manager` リポジトリと本セッションの実作業のみに基づく。
> **SSOT への転記が必要**（末尾「SSOT 転記用ブロック」参照）。

## 環境
- リモート実行環境（Claude Code on the web）。コンテナは使い捨て、リポジトリのみクローン済み
- 作業対象: `fukukei23/atelier-kyo-manager` のみ。obsidian-ssot / claude-config 等の並行リポジトリは**未クローン**
- Python 3.11 / venv は都度構築（`python3 -m venv venv && ./venv/bin/pip install -r requirements.txt`）
- ローカル WSL 環境の自動化機構（auto-sync / auto-push 等）は本環境では**一切動作していない**。
  実態は `00_SYSTEM/自動化.md` を正典とすること

## 前回セッションまでの状態
- **包括的リポジトリレビューを実施し 14 件を指摘**。`docs/REVIEW_ISSUES_2026-08-12.md` に issue-backlog 形式で記録
- **最重要発見（P0 ISSUE-101）**: テスト 2,151 件が緑なのに、`calculate_pricing()` の呼び出し元 6 経路が
  `PricingInput` を price source 未設定（`UNKNOWN`）で構築しており `PriceIntegrityError` で必ず失敗する。
  `POST /orders/new` / `/orders/<id>/edit` の**注文登録が保存されずに終わる**（`@handle_db_error` が例外を握るため無音）。
  `run_pipeline()` と `Order.calc_profit()` を実際に実行して再現確認済み
- **P0 ISSUE-102**: 上記バグをテスト側がモックで隠蔽していた。
  `tests/test_sourcing_profitability_coverage.py` に「PriceIntegrityError を raise するため、テストではモック化」と明記あり
- **14 件中 ISSUE-114 のみ修正済み**: `overrides_store.py` の `BACKUP_DIR` が CWD 依存のハードコード相対パスで、
  起動ディレクトリ次第でバックアップ先が散り `MAX_BACKUPS=50` のローテーションが機能しない状態だった。
  `.gitignore:189` の破損行と、混入済み `.bak` 18 件も併せて解消
- **draft PR #99 を作成・維持**。main の `44badba`（Celery 配線修復）を取り込む際に発生した
  `docs/変更履歴.md` のコンフリクトを解消済み。CI 両ジョブ green / mergeable_state clean / レビューコメント 0 件

## 前回占有タスク（継続可・参考）
- **PR #99 の扱い**: draft のまま未マージ。マージするか P0 修正まで待つかは未決（経営者判断）
- **PR #99 の watch**: 本セッションが `subscribe_pr_activity` で監視中。定期チェックインを再設定し続けている。
  **新セッションで作業を引き継ぐ場合、二重に触らないよう watch の扱いを決めること**
- **ISSUE-101 の修正**: 未着手。次セッションの本命タスク

## WIP構想一覧（未spec化の構想）
`00_SYSTEM/バックログ.md` がリモート環境に存在せず**機械スキャン不可**のため、本欄は取得できていない。
次回 resume-session 時にローカルでバックログを直接確認すること。
（本セッション由来の未 spec 構想としては「テスト設計方針の見直し」が該当 — 下記参照）

- **title**: 「部品は動くがつなぎ目が切れている」型障害に対するテスト方針
  **方針**: ISSUE-101/102 と main の `44badba`（Celery）が同一構造。単体テストでは原理的に検出できない。
  結合テストをどこに置くかの方針決めが必要。Celery 側は `tests/test_celery_tasks.py` にレジストリ検証を
  置く形で解決済みなので、それに倣うのが自然。ISSUE-101 修正時に併せて決めること

## 次のタスク
**バックログ.md を参照のこと**（生きタスクの唯一の正典）。本 handoff には候補を列挙しない。

ただし本セッションで新規に積んだ 13 件は**まだ SSOT に転記されていない**ため、
`docs/引き継ぎ_2026-08-13_レビュー後バックログ.md` 末尾の「SSOT 転記用ブロック」を
`00_SYSTEM/バックログ.md` に貼り付けてから resume-session を回すこと。

推奨着手順（詳細は上記引き継ぎドキュメントに修正対象ファイル・行番号・完了条件つきで記載）:
1. **ISSUE-101 → 102 → 111** を第1ブロックで（すべて pricing 層・1〜2日）
2. ISSUE-104 / 105（外部公開前の必須項目・3時間）
3. ISSUE-103（BUYMA 公式で決済手数料を確定させるまで着手不可）

## 必ず読むファイル
| パス | 内容 |
|---|---|
| `docs/引き継ぎ_2026-08-13_レビュー後バックログ.md` | **最優先**。着手順・修正対象・完了条件・SSOT 転記用ブロック |
| `docs/REVIEW_ISSUES_2026-08-12.md` | 14 件の指摘全文（再現手順つき） |
| `docs/経営者判断.md` | Layer A。手数料・関税率・利益閾値。特に §9（未解決事項）は ISSUE-103 の前提 |
| `docs/要件定義.md` | Layer A |
| `docs/変更履歴.md` | 2026-08-12 / 08-13 エントリ |
| `CLAUDE.md` | ドキュメント読込ルール・価格調査の鉄則 |

## 注意事項
- **`CLAUDE.md` の Layer A（`経営者判断.md` / `要件定義.md`）を実装前に必ず読むこと。**
  既に記載済みの経営判断を再質問しないこと
- **「テストが緑」を完了条件にしない。** 本セッションの最大の発見がこれ。
  ISSUE-101 の修正は「本番経路をモックなしで1回通す」ことを完了条件とすること
- **推測データでの価格調査は厳禁**（`CLAUDE.md` 価格調査の鉄則）。`PriceSource` の設計は正しいので、
  ISSUE-101 は安全装置を緩める方向ではなく**デフォルト値 `UNKNOWN` を廃止して必須引数化する**方向で直すこと
- **ブランチ**: `claude/atelier-kyo-manager-review-xyjbb2`（head `94fd2fa`）。
  PR #99 は draft・未マージ。新規作業を積むならこのブランチか、main から切り直すか先に決めること
- **リモート環境では SSOT に書けない。** 本 handoff と 08-13 の引き継ぎは
  リポジトリ側に暫定保存してある。ローカルで SSOT へ転記したら status を更新するか削除してよい

---

## SSOT 転記用ブロック

### → `00_SYSTEM/handoff/2026-08-16_0007_07bc.md`
本ファイルをそのままコピーする。

### → `00_SYSTEM/バックログ.md`
`docs/引き継ぎ_2026-08-13_レビュー後バックログ.md` 末尾の「SSOT 転記用ブロック」を参照（13 件のチェックリスト）。

### → `01_DECISIONS/atelier-kyo-manager/2026-08-12_包括レビューとPriceIntegrityError配線漏れの発見.md`
同じく `docs/引き継ぎ_2026-08-13_レビュー後バックログ.md` 末尾に記載済み。

### → `00_SYSTEM/active-sessions.md`
本セッション（wt4 = `07bc`）の 🟢 行を ✅ に変更すること。
リモート環境では active-sessions.md にアクセスできず、Step 5 を実行できていない。
